# not using as of now
version: '3.8'

services:
  client:
    build:
      context: ./client
    ports:
      - "80:80" 
    depends_on:
      - server

  server:
    build:
      context: ./server
    ports:
      - "3001:3001"  
    environment:
      - NODE_ENV=production
